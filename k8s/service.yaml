apiVersion: v1
kind: Service

metadata:
  name: vehicle-sales-service

spec:
  type: NodePort

  selector:
    app: vehicle-sales-service

  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30081